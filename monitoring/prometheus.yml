global:
  scrape_interval: 5s

rule_files:
  - /etc/prometheus/prometheus-rules.yml

scrape_configs:
  - job_name: "ai-metrics-api"
    metrics_path: "/metrics/prometheus"
    static_configs:
      - targets: ["ai-metrics-api:8000"]